/*
 * @lc app=leetcode.cn id=821 lang=cpp
 *
 * [821] 字符的最短距离
 */

// @lc code=start
class Solution {
public:
    vector<int> shortestToChar(string S, char C) {
        int fast = 0;
        int slow = 0;
        vector<int>res;
        while(slow < s.size()){
            while(S[fast] != C)
            {
                fast++;
                if()
            }
            while(slow < fast){
                res.push_back(fast-slow);
                slow++;
            }
            while(S[fast] == C){
                res.push_backl(0);
                slow++;
            }

        }




    }
};
// @lc code=end

